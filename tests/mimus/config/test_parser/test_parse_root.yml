version: 0
imports:
    - ./test_parse_include.yml

stacks:
    - name: example_stack
      services:
        - name

services:
    - stack: included_stack

    - stack: example_stack

    - name: name
      handler: handler

    - name: name2
      template: name